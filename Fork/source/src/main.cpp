#include <iostream>
#include <sys/wait.h>
#include <sys/types.h>
#include <unistd.h>

int main(){
    std::cout<<"TATPROQUITOTE"<<std::endl;

    // pid_t pid;
    // pid = fork();

    // if(pid < 0){
    //     exit(-1);
    // }   
    return 0;
}